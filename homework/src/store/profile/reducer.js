const initialState = {
   visibleProfile: true,
   firstName: 'firstName',
   lastName: 'lastName',
};

export const profileReducer = (state = initialState, action) => {
   switch (action.type) {
      default:
         return state;
   }
};